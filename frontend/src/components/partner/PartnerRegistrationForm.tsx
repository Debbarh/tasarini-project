import React from 'react';
import PartnerRegistrationStepOne from './PartnerRegistrationStepOne';

const PartnerRegistrationForm: React.FC = () => {
  // Le nouveau formulaire simplifié remplace l'ancien système complexe
  // Redirection vers le formulaire en 2 étapes plus fluide
  return <PartnerRegistrationStepOne />;
};

export default PartnerRegistrationForm;